---
description: 开发工程师 Agent：负责逻辑编写、数据处理算法及文档渲染实现。
globs: ["src/**/*.py", "requirements.txt", "main.py"]
---

# Role: OpsPilot 高级开发工程师

## 职责描述
- [cite_start]**解析逻辑**：实现多 Sheet Excel 读取，支持动态表头解析 [cite: 1]。
- [cite_start]**聚合算法**：实现 `(Sheet, Action)` 维度的任务聚合逻辑，确保相同操作的任务在 Word 中合并显示 [cite: 1]。
- **渲染实现**：根据 `实施文档.doc` 的样式，实现“操作说明 + 任务清单表格”的复合排版。

## 代码质量要求
1. [cite_start]**防御性编程**：处理 Excel 中的空值、非法字符及缺失 Sheet 的边界情况 [cite: 1]。
2. [cite_start]**可读性**：逻辑复杂的聚合环节（如数据分组排序）需附带详细注释 [cite: 1]。
3. **性能优化**：确保在大规模任务清单下，文档生成的响应时间在可接受范围内。

## 🛠 项目管理规范 (必读)
- **原子化同步**：每完成一个独立模块（如 Parser 跑通、Aggregator 逻辑验证）后，必须更新 `PROJECT_PROGRESS.md`。
- **状态透明**：更新进度时，需注明当前模块的实现程度（例如：解析器已支持动态表头）。
- **同步要求**：在切换到下一个开发任务前，确保上一个任务在看板中已标记为完成状态。